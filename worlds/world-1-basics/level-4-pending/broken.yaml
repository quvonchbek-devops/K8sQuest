apiVersion: v1
kind: Pod
metadata:
  name: hungry-app
  namespace: k8squest
spec:
  containers:
  - name: app
    image: nginx:latest
    resources:
      requests:
        memory: "999Gi"
        cpu: "999"

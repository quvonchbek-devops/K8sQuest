🔍 Подсказка-наблюдение:

Проверьте список подов: `kubectl get pods`

Pod вообще отсутствует! В отличие от локального кластера, у этого namespace
свои лимиты ресурсов, и они проверяются ДО создания Pod.

Примените broken.yaml сами:

  kubectl apply -f broken.yaml

Внимательно прочитайте сообщение об ошибке — оно прямо называет проблему.

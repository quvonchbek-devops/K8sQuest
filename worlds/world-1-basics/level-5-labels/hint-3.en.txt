💡 Near-solution hint:

The Service selector says `app: frontend`, but the pod has the label `app: backend`. They do not match!

Steps:
1. Delete the resources: `kubectl delete -f broken.yaml`
2. In broken.yaml change the Service selector from `app: frontend` to `app: backend`
3. Apply: `kubectl apply -f broken.yaml`
4. Check the endpoints: `kubectl get endpoints backend-service -n k8squest`

An IP address should appear as an endpoint!

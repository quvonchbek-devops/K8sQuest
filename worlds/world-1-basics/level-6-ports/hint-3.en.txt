💡 Near-solution hint:

Steps:
1. Delete the resources: `kubectl delete -f broken.yaml`
2. In broken.yaml change the Service targetPort from 8080 to 80
3. Apply: `kubectl apply -f broken.yaml`
4. Check: `kubectl get endpoints web-service -n k8squest`
   The endpoint should show the pod IP and port 80. (`port-forward` is not
   available in this terminal — it needs a continuous tunnel.)

💡 Подсказка почти с решением:

Шаги:
1. Удалите ресурсы: `kubectl delete -f broken.yaml`
2. В broken.yaml измените targetPort у Service с 8080 на 80
3. Примените: `kubectl apply -f broken.yaml`
4. Проверьте: `kubectl get endpoints web-service -n k8squest`
   В endpoints должны появиться IP Pod и порт 80. (`port-forward` в этом
   терминале недоступен — ему нужен постоянный туннель.)

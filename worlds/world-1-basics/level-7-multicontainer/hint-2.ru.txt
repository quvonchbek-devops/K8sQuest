🧭 Направляющая подсказка:

Контейнер "log-sidecar" падает. Чтобы понять почему:

`kubectl logs app-with-logging -c log-sidecar -n k8squest`

Ошибка показывает, что файла `/var/log/nonexistent.log` не существует. `tail -f` сразу завершается, если файла нет, и контейнер падает.

Команду нужно изменить:
1. Либо сначала создать файл,
2. Либо использовать команду, которой не нужен готовый файл

💡 Near-solution hint:

Steps:
1. Delete the pod: `kubectl delete pod app-with-logging -n k8squest`
2. In broken.yaml change the log-sidecar command to one that works:
   ```
   command: ["sh", "-c", "while true; do echo 'Logging...' >> /var/log/app.log; sleep 5; done"]
   ```
3. Apply: `kubectl apply -f broken.yaml`
4. Check that both containers run: `kubectl get pod app-with-logging -n k8squest`

The READY column should read "2/2"!

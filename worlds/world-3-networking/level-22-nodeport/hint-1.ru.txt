🔍 Подсказка-наблюдение:

Проверьте детали Service: `kubectl get svc web-nodeport -n k8squest`

Вы увидите назначенный NodePort (случайный порт из диапазона 30000-32767).

Проблема? Он случайный! Для предсказуемого доступа нужен явный NodePort.

Посмотрите на YAML Service — указан ли nodePort в секции ports?

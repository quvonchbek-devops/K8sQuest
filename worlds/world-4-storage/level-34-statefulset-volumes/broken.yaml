---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-storage  # ❌ Single PVC for all pods!
  namespace: k8squest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Bu PVC ni PROVISIONER bog'laydi (level o'z PersistentVolume ini
  # yaratmaydi), ya'ni bu yerda KLASTERDA HAQIQATAN MAVJUD sinf turishi
  # shart. Ilgari `standard` yozilgan edi — u kind/minikube nomi, k3s da
  # esa yo'q: PVC abadiy Pending da qolar, pod hech qachon ko'tarilmas va
  # darsning O'Z nuqsoni umuman ko'rinmasdi.
  #
  # Nomni yodlamang: `kubectl get storageclass` bilan so'rang.
  storageClassName: local-path
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-cluster
  namespace: k8squest
spec:
  serviceName: postgres
  replicas: 3
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:14
        env:
        - name: POSTGRES_PASSWORD
          value: secretpassword
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: database-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: database-storage
        persistentVolumeClaim:
          claimName: database-storage  # ❌ All pods share same PVC!
  # ❌ Missing volumeClaimTemplates!

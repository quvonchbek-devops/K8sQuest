🔍 Подсказка 1: Наблюдение за проблемой

С данными Secret что-то не так.

Проверьте:
1. `kubectl get secret -n k8squest -o yaml` — посмотрите содержимое Secret
2. Раскодируйте значения поля `data` из base64:
   `echo "значение" | base64 -d`
3. Проверьте логи пода: `kubectl logs database-client -n k8squest`

Правильно ли закодированы значения Secret? Или они записаны обычным текстом?

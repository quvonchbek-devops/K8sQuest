🔍 Подсказка 1: Наблюдение за проблемой

Под работает, но запись в том не удаётся — «Permission denied».

Проверьте:
1. `kubectl logs writer-app -n k8squest` — посмотрите ошибку доступа
2. `kubectl get pods -n k8squest -o jsonpath='{.items[*].spec.securityContext}'`
3. От какого пользователя (UID) работает контейнер?
4. Какому пользователю принадлежит файловая система тома?

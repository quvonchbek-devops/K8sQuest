✅ Maslahat 3: To'liq Yechim

broken.yaml ga securityContext qo'shing:

```yaml
spec:
  securityContext:
    fsGroup: 1000          # ✅ Volume ruxsatlari uchun
  containers:
  - name: app
    securityContext:
      runAsUser: 1000      # ✅ Konteyner foydalanuvchisi
```

`kubectl delete -f broken.yaml && kubectl apply -f broken.yaml`
`kubectl logs -n k8squest` — "Permission denied" yo'qolishi kerak

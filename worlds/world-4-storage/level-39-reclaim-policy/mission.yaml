level: 39
title: "Reclaim Policy Data Loss"
objective: "Buzilgan resurslarni tuzating va validatsiyadan o'tkazing"
name: "Reclaim Policy Ma'lumot Yo'qotishi"
world: "Storage & Stateful Apps"
difficulty: intermediate
xp: 250
expected_time: 15m
concepts:
  - reclaim-policy
  - persistent-volume
  - data-retention
  - delete-vs-retain
description: "PersistentVolume ning reclaim policy si 'Delete' ga sozlangan, bu PVC o'chirilganda ma'lumotlar yo'qotilishiga olib kelishi mumkin."
learning_objectives:
  - Understand PV reclaim policies
  - Learn difference between Delete and Retain
  - Prevent accidental data loss
  - Configure appropriate retention policies

# Bu level HOSTED ning ikki turida BOSHQACHA — shuning uchun overlay ZANJIR.
#
# `hosted` (v1, namespace sandbox): O'YNALMAYDI. Foydalanuvchi bitta
# izolyatsiyalangan namespace da, namespace-scoped RBAC bilan ishlaydi —
# cluster-scoped resurs ham, ikkinchi namespace ham mumkin emas.
#
# `hosted-cluster` (v2, nested klaster): O'YNAYDI. Foydalanuvchi O'Z
# klasterining cluster-admin i, to'qnashadigan qo'shni yo'q.
# O'LCHANDI (taxmin emas): L39 — validate rc=0.
#
# Zanjir: `hosted` ikkalasiga, `hosted-cluster` uning ustiga (faqat farq).
# Batafsil: docs/MISSION-SCHEMA.md, services/content/environment.go
environments:
  hosted:
    # Tavsif overlay da almashtirilgan — tarjimasi ham shu yerda.
    i18n:
      en:
        description: "This lesson teaches PersistentVolume reclaim policies at cluster level. The hosted sandbox has namespace-scoped RBAC. This lesson runs only in the local K8sQuest game."
      ru:
        description: "Этот урок учит reclaim policy у PersistentVolume на уровне кластера. У хостед-песочницы RBAC ограничен namespace. Этот урок доступен только в локальной игре K8sQuest."
    available: false
    description: "Bu level cluster darajasidagi PersistentVolume reclaim siyosatini o'rgatadi. Hosted sandbox scoped RBAC bilan cluster-scoped resurs yaratolmaydi. Dars faqat lokal o'yinda o'ynaladi."
  hosted-cluster:
    available: true

# Tarjimalar — bu level hosted da o'ynalmaydi, lekin KATALOGDA ko'rinadi.
i18n:
  en:
    name: "Reclaim Policy Data Loss"
  ru:
    name: "Потеря данных из-за reclaim policy"

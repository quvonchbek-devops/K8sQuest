level: 44
title: "NetworkPolicy ni: Traffic Blocked"
objective: "Buzilgan resurslarni tuzating va validatsiyadan o'tkazing"
name: "NetworkPolicy ni Izolyatsiyasi"
difficulty: advanced
xp: 350
expected_time: 25m
xp_reward: 350
world: 5
concepts:
  - networkpolicy
  - ingress
  - egress
  - pod-selector
description: "NetworkPolicy ni pod lar o'rtasidagi zarur aloqani to'sib qo'ymoqda. Xavfsizlikni saqlab, kerakli trafikka ruxsat beradigan qilib tuzatish kerak."
learning_objectives:
  - Understand NetworkPolicy ni ingress/egress rules
  - Configure pod selectors and namespace selectors
  - Allow specific ports while blocking others
  - Debug network connectivity issues

# Bu level HOSTED ning ikki turida BOSHQACHA — shuning uchun overlay ZANJIR.
#
# `hosted` (v1, namespace sandbox): O'YNALMAYDI. Foydalanuvchi bitta
# izolyatsiyalangan namespace da, namespace-scoped RBAC bilan ishlaydi —
# cluster-scoped resurs ham, ikkinchi namespace ham mumkin emas.
#
# `hosted-cluster` (v2, nested klaster): O'YNAYDI. Foydalanuvchi O'Z
# klasterining cluster-admin i, to'qnashadigan qo'shni yo'q.
# O'LCHANDI (taxmin emas): L44 — NetworkPolicy kuchga kirdi — validate rc=0.
#
# Zanjir: `hosted` ikkalasiga, `hosted-cluster` uning ustiga (faqat farq).
# Batafsil: docs/MISSION-SCHEMA.md, services/content/environment.go
environments:
  hosted:
    available: false
    description: "Bu level bir nechta namespace o'rtasidagi NetworkPolicy ni o'rgatadi. Hosted sandbox — bitta izolyatsiyalangan namespace. Dars faqat lokal o'yinda o'ynaladi."
  hosted-cluster:
    available: true
